{
  "name": "tetris-beats-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker backend (leaderboard + PvP signaling) for Tetris Beats.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
